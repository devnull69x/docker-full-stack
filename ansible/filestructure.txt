F:\github\docker-full-stack\ansible\
├── inventories/
│   └── production/
│       ├── hosts.ini               # ✅ EXISTS (3 nodes defined)
│       └── group_vars/
│           └── alpine_nodes.yml    # ✅ EXISTS (vars for cluster)
├── roles/
│   ├── alpine_base/
│   │   ├── tasks/
│   │   │   ├── main.yml           # ✅ EXISTS (base setup)
│   │   │   ├── timezone.yml       # ✅ EXISTS (Asia/Singapore)
│   │   │   └── motd.yml           # ✅ EXISTS (banner cleanup)
│   │   └── templates/
│   │       └── hosts.j2           # ✅ EXISTS (dynamic /etc/hosts)
│   │
│   ├── alpine_hardening/          # 🔄 CONFIRMED STRUCTURE
│   │   ├── tasks/
│   │   │   ├── sshd.yml           # ✅ EXISTS (SSH config)
│   │   │   ├── fail2ban.yml       # ✅ EXISTS (Fail2Ban setup)
│   │   │   └── sudoers.yml        # ✅ EXISTS (sudo rules)
│   │   └── templates/             # ✅ ORIGINALLY EMPTY
│   │       ├── sshd_config.j2     # ✅ ADDED (no path change)
│   │       ├── banner.j2          # ✅ ADDED (no path change)
│   │       └── jail.local.j2      # ❗ ADDED (but dir existed)
│   │
│   ├── user_admin/
│   │   ├── tasks/
│   │   │   ├── main.yml           # ✅ EXISTS (user creation)
│   │   │   └── keys.yml           # ✅ EXISTS (SSH keys)
│   │   └── vars/
│   │       └── main.yml           # ✅ EXISTS (user vars)
│   │
│   └── docker_ce/
│       └── tasks/
│           └── main.yml           # ✅ EXISTS (Docker install)
│
├── playbooks/
│   ├── provision.yml              # ✅ EXISTS (main playbook)
│   └── secure.yml                 # ✅ EXISTS (hardening playbook)
│
├── group_vars/
│   └── all.yml                    # ✅ EXISTS (timezone: Asia/Singapore)
│
├── ansible.cfg                    # ✅ EXISTS (config)
└── README.md                      # ✅ EXISTS (documentation)