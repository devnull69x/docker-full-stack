# Test dependencies for Alpine Hardening Automation
ansible>=2.12
ansible-lint>=6.0
yamllint>=1.26
molecule>=3.5
docker>=5.0
python>=3.8
jq>=1.6
testinfra>=6.0
pytest>=6.0

# Optional dependencies for advanced testing
pytest-xdist>=2.0  # Parallel testing
pytest-html>=3.0   # HTML test reports

#pip install -r tests/requirements.txt